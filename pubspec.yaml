name: test_task_flutter
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # di:
  get_it: ^8.0.3
  injectable: ^2.5.0

  # routing:
  auto_route: ^10.0.1

  # network:
  dio: ^5.8.0+1
  logger: ^2.5.0
  retrofit: ^4.4.2

  # state management (Bloc):
  flutter_bloc: ^9.1.0

  # common boilerplate generators:
  equatable: ^2.0.7
  freezed: ^3.0.6
  json_annotation: ^4.9.0

  # local storage
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  #di
  injectable_generator: ^2.7.0

  # routing
  auto_route_generator: ^10.0.1

  # network
  retrofit_generator: ^9.1.9

  # common boilerplate_generators:
  freezed_annotation: ^3.0.0
  json_serializable: ^6.9.4

  build_runner: ^2.4.15

flutter:
  uses-material-design: true